import styled from 'styled-components'

const Because = styled.span`
  font-family: 'Roboto', sans-serif;
  font-weight: 700;
  font-size: 14px;
  color: #00D092;
  line-height: 20px;
`

export default Because

