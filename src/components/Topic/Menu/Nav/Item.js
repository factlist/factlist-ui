import styled from 'styled-components'

const Item = styled.div`
  cursor: pointer;
  padding: 8px 15px;

  font-size: 14px;
  font-weight: 400;

  &:hover {
    background-color: rgba(67, 90, 111, 0.04);
  }
`

export default Item
