import styled from 'styled-components'

const Name = styled.span`
  font-size: 13px;
  color: rgba(0,0,0,0.85);
  letter-spacing: 0;
  font-weight: 700;
  display: block;
  margin-top: 13px;
`

export default Name
