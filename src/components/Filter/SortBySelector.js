import styled from 'styled-components'
import { SortBy} from 'react-instantsearch-dom';

const SortBySelector = styled(SortBy)`
   
  font-size:13px;
  margin-bottom:15px;
  text-align:right;
`

export default SortBySelector
