import styled from 'styled-components'

const H2 = styled.h2`
  font-size: 24px;
  font-weight: normal;
  color: rgba(0,0,0,0.90);
  letter-spacing: 0;
  text-align: center;
  margin: 0;
`

export default H2
